openapi: 3.0.0
paths:
  /api:
    get:
      operationId: AppController_getHello
      parameters: []
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                type: string
                example: Hello from Analysis API!
      summary: Hello world
      tags:
        - App
  /api/health:
    get:
      description: Возвращает статус сервиса и текущее время сервера
      operationId: AppController_getHealth
      parameters: []
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                example:
                  status: ok
                  timestamp: '2026-05-03T14:00:00.000Z'
      summary: Health check
      tags:
        - App
  /api/auth/login:
    post:
      description: Возвращает пару access/refresh токенов
      operationId: AuthController_login
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginDto'
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseDto'
        '401':
          description: Неверный email или пароль
      summary: Вход в систему
      tags:
        - Auth
  /api/auth/refresh:
    post:
      description: Принимает refresh token, возвращает новую пару. Старый токен аннулируется (ротация)
      operationId: AuthController_refresh
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenDto'
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokensResponseDto'
        '401':
          description: Токен недействителен, истёк или уже был использован
      summary: Обновление токенов
      tags:
        - Auth
  /api/auth/logout:
    post:
      description: Аннулирует переданный refresh token. Требует валидный access token в заголовке Authorization
      operationId: AuthController_logout
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenDto'
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                example:
                  message: Logged out successfully
        '401':
          description: Access token отсутствует или недействителен, либо refresh token не найден
      security:
        - access-token: []
      summary: Выход из системы
      tags:
        - Auth
  /api/dialogs:
    post:
      description: >-
        Студент создаёт диалог с преподавателем и загружает первую версию работы. Одновременно создаётся Submission v1 и
        запись File с путём к сохранённому файлу.
      operationId: DialogsController_create
      parameters: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - teacherId
                - title
                - file
              properties:
                teacherId:
                  type: string
                  format: uuid
                title:
                  type: string
                  example: Курсовая работа по алгоритмам
                comment:
                  type: string
                  example: Первая версия
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateDialogResponseDto'
        '401':
          description: Требуется авторизация
      security:
        - access-token: []
      summary: Создать диалог
      tags:
        - Dialogs
info:
  title: Analysis API
  description: API системы проверки студенческих работ на соответствие ГОСТ
  version: '1.0'
  contact: {}
tags: []
servers: []
components:
  securitySchemes:
    access-token:
      scheme: bearer
      bearerFormat: JWT
      type: http
  schemas:
    LoginDto:
      type: object
      properties:
        email:
          type: string
          example: teacher@example.com
        password:
          type: string
          example: teacher-password
      required:
        - email
        - password
    AuthTokensResponseDto:
      type: object
      properties:
        accessToken:
          type: string
          description: JWT access token (15m)
        refreshToken:
          type: string
          description: JWT refresh token (7d)
      required:
        - accessToken
        - refreshToken
    RefreshTokenDto:
      type: object
      properties:
        refreshToken:
          type: string
          description: Refresh token, полученный при login или refresh
      required:
        - refreshToken
    DialogDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        studentId:
          type: string
          format: uuid
        teacherId:
          type: string
          format: uuid
        title:
          type: string
          example: Курсовая работа по алгоритмам
        status:
          type: string
          enum:
            - open
            - approved
            - rejected
            - closed
          example: open
        createdAt:
          format: date-time
          type: string
      required:
        - id
        - studentId
        - teacherId
        - title
        - status
        - createdAt
    DialogSubmissionDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        dialogId:
          type: string
          format: uuid
        version:
          type: number
          example: 1
        comment:
          type: string
        createdAt:
          format: date-time
          type: string
      required:
        - id
        - dialogId
        - version
        - createdAt
    DialogFileDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        originalName:
          type: string
        path:
          type: string
          description: 'Относительный путь для получения файла: GET /api/files/:path'
        mimeType:
          type: string
          example: application/pdf
        size:
          type: number
          description: Размер в байтах
        createdAt:
          format: date-time
          type: string
      required:
        - id
        - originalName
        - path
        - createdAt
    CreateDialogResponseDto:
      type: object
      properties:
        dialog:
          $ref: '#/components/schemas/DialogDto'
        submission:
          $ref: '#/components/schemas/DialogSubmissionDto'
        file:
          $ref: '#/components/schemas/DialogFileDto'
      required:
        - dialog
        - submission
        - file
